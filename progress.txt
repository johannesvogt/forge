2026-06-20 Issue #20 — Project API routes
  - Created lib/api/projects.ts: parseCreateProjectBody (validates name, trims) and formatProject (returns id, name, slug, createdAt only).
  - Added lib/api/projects.test.ts: 9 unit tests covering null/missing/non-string/empty/whitespace-only name, valid name with trim, response field filtering.
  - Created app/api/projects/route.ts: GET lists all projects; POST validates name, creates project via project-service, returns 201; 409 on SlugConflictError; 401 without session.
  - Created app/api/projects/[slug]/route.ts: GET returns project by slug or 404; DELETE deletes by slug (404 if not found, 204 on success); 401 without session.
  - API key routes (POST/GET /api/account/api-keys) already completed in issue #17.
  - 247 tests pass; tsc --noEmit passes with zero errors.

2026-06-20 Issue #19 — MCP project scoping
  - createMcpServer(db, agentLabel, projectId) and mcp/index.ts projectId exit guard already in place from issue #18.
  - All 18 MCP tools confirmed forwarding projectId to the service layer.
  - Added cross-project isolation test to mcp/server.test.ts: two in-memory servers with different projectIds; issue created on server B is not visible on server A.
  - 238 integration tests pass; tsc --noEmit passes with zero errors.

2026-06-19 Issue #18 — Domain services project scoping
  - Updated issue-service, document-service, diff-service, skill-service, context-service: all public functions gained projectId as 2nd param; all DB queries scoped by projectId.
  - context-service: removed singleton pattern; switched to per-project upsert via ON CONFLICT ("projectId").
  - skill-service.getSkillByName: uses compound unique key { projectId_name: { projectId, name } }.
  - Cross-project isolation tests added to every service (data in project A not visible from project B).
  - mcp/server.ts: createMcpServer(db, agentLabel, projectId) — projectId fixed at startup, passed to all service calls.
  - mcp/index.ts: extracts projectId from findActiveApiKey; exits with error if projectId is falsy.
  - 17 API route files updated with const projectId = '' placeholder + TODO(issue #20) comment.
  - 237 integration tests pass; tsc --noEmit passes with zero errors.

2026-06-19 Issue #17 — Project-scoped API keys
  - Updated createApiKey signature: added required projectId parameter, passed to Prisma INSERT.
  - Updated findActiveApiKey: now returns { userId, label, projectId }.
  - listApiKeys returns ApiKey[] which now includes projectId (Prisma client regenerated).
  - Updated app/api/account/api-keys/route.ts: POST requires projectId in body; GET includes projectId in response.
  - Regenerated Prisma client so ApiKey type reflects schema.
  - Test setup creates two test projects per run; all createApiKey calls pass testProjectId.
  - 12 integration tests pass: original tests updated + listApiKeys includes projectId + findActiveApiKey returns correct projectId + cross-project isolation test.
  - tsc --noEmit passes with zero errors.

2026-06-19 Issue #16 — Project service
  - Created lib/projects/project-service.ts: createProject, listProjects, getProject, deleteProject, SlugConflictError.
  - Slug generation: lowercase, spaces→hyphens, strip non-alphanumeric, collapse consecutive hyphens.
  - createProject seeds default skills into the project and creates an empty ProjectContext.
  - Updated lib/skills/seed-skills.ts: seedDefaultSkills now requires projectId; uses compound unique key projectId_name.
  - Removed stale global seedDefaultSkills call from app/api/skills/route.ts (will be replaced in issue #20).
  - 7 integration tests pass: create, slug format, list order, get-by-slug (found + null), delete cascade, slug conflict.
  - tsc --noEmit passes with zero errors.

2026-06-19 Issue #15 — Project schema
  - Deleted stale migration (20260619204655_init) and generated fresh migration (20260619212541_init) from current schema.prisma.
  - schema.prisma already had Project model, projectId FKs with CASCADE DELETE on all 6 models, ProjectContext@@unique([projectId]), Skill@@unique([projectId, name]).
  - Migration applied cleanly to fresh Postgres 16 database; all 13 tables created correctly.
  - tsc --noEmit passes with zero errors.
  - Existing service tests fail with not-null constraint violations on projectId — expected, to be fixed in issues #17/#18.
