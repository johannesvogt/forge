2026-06-19 — Issue 13: Skill Registry

Humans manage prompt-based skills via the web UI; agents load them read-only via MCP. Five default skills seeded on first load.

Deliverables:
- prisma/schema.prisma — Skill model (id, name unique, description, prompt, timestamps) and SkillFile model (id, skillId cascade FK, name, content, createdAt; unique [skillId, name]); db push + client regenerated
- lib/skills/skill-service.ts — Skill and SkillFile interfaces; Db interface for testability; createSkill, getSkillByName, getSkillById, listSkills, updateSkill, deleteSkill, addSkillFile, getSkillWithFiles, deleteSkillFile
- lib/skills/skill-service.test.ts — 16 TDD integration tests across 7 suites covering create, lookup by name/id, list, update, delete, file attachment, and file removal
- lib/skills/seed-skills.ts — seedDefaultSkills idempotently creates the 5 default skills (to-prd, to-issues, grill-with-docs, tdd, improve-codebase-architecture) with full Forge-MCP-adapted prompts
- app/api/skills/route.ts — GET list (auto-seeds defaults on first call) + POST create (human session)
- app/api/skills/[id]/route.ts — GET detail with files, PATCH update, DELETE (human session)
- app/api/skills/[id]/files/route.ts — POST add supporting file (human session)
- app/api/skills/[id]/files/[fileId]/route.ts — DELETE remove file (human session)
- app/skills/page.tsx — lists skills with name, description, date; inline create form; links to detail page
- app/skills/[id]/page.tsx — shows name, description, primary prompt; inline edit form; delete with confirm; supporting files panel with add/remove
- mcp/server.ts — list_skills() returns [{name, description}]; get_skill(name) returns {skill, files}; isError on unknown name; no write tools for agents
- mcp/server.test.ts — makePgClient extended with skill/skillFile; cleanup in after(); 6 new integration tests: list_skills (name+description, no prompt in list), get_skill (prompt+files, empty files, isError on unknown)
- 200/200 tests passing; tsc --noEmit clean

2026-06-19 — Issue 12: MCP Gateway — Diffs

Extended MCP server with three diff tools and verified diff line comment support end-to-end via the existing add_comment/list_comments tools.

Deliverables:
- mcp/server.ts — three new tools: upload_diff(title, description?, branch, diff_text, issue_id), get_diff(id), list_diffs(issue_id); thin wrappers over existing diff-service functions; agent label recorded as authorLabel
- mcp/server.test.ts — makePgClient extended with diff table implementation (create/findUnique/findMany); after() extended to delete test diffs; 10 new TDD integration tests: upload_diff (id/title/branch/diffText/authorLabel returned, optional description stored), get_diff (all fields returned, isError on unknown id), list_diffs (chronological order, empty array, isolation across issues), add_comment/list_comments on diff lines (anchor stored, filtered by file_path/line_number, isolation across diffs)
- 179/179 tests passing; tsc --noEmit clean

2026-06-19 — Issue 11: MCP Gateway — Documents

Extended MCP server with four document tools so agents can manage the full document lifecycle.

Deliverables:
- lib/documents/document-service.ts — versionId: string added to Document interface; populated in createDocument, getDocument, getDocumentAtVersion, updateDocument so agents can use it as target_id for inline comments
- mcp/server.ts — four new tools: create_doc(title, content, issue_id), get_doc(id, version?), update_doc(id, content), list_docs(issue_id); thin wrappers over existing document-service functions; agent label recorded as authorLabel
- mcp/server.test.ts — makePgClient extended with document/documentVersion/documentIssueLink table implementations; after() extended to delete test documents; TEST_DOC_ISSUE_ID constant; 8 new TDD integration tests: create_doc (id/title/content/versionNumber/versionId), get_doc latest/specific-version/isError-on-missing, update_doc version-2-returned/v1-unchanged, list_docs two-docs-returned/empty-array
- 169/169 tests passing; tsc --noEmit clean

2026-06-19 — Issue 10: MCP Gateway — Issues and Comments

Foundational MCP server exposing issue tracker and comment system to agents via the Model Context Protocol.

Deliverables:
- package.json — @modelcontextprotocol/sdk ^1.29.0 and zod ^4.4.3 added; test script extended to 'lib/**/*.test.ts' 'mcp/**/*.test.ts'
- mcp/server.ts — createMcpServer(db, agentLabel) factory; 7 tools: list_issues, get_issue, create_issue, update_issue, move_issue (state machine enforced), list_comments (anchor support), add_comment (agent author); thin translation layer over existing services
- mcp/index.ts — StdioServerTransport entry point; validates MCP_API_KEY env var via findActiveApiKey; exits on missing/revoked key
- mcp/server.test.ts — 15 TDD integration tests using InMemoryTransport + real PostgreSQL: list_issues all/filtered, create_issue defaults/column, get_issue found/not-found isError, update_issue, move_issue valid/invalid-transition isError, add_comment agent-author, list_comments chronological/empty, auth findActiveApiKey valid/invalid/empty
- 161/161 tests passing; tsc --noEmit clean

2026-06-19 — Issue 9: Diff Line-Level Comments

Line-level comments anchored to (file_path, line_number, diff_id) tuples in the diff viewer.

Deliverables:
- prisma/schema.prisma — anchorFilePath String? added to Comment model; db push + client regenerated
- lib/comments/comment-service.ts — DiffLineAnchor interface { filePath, lineNumber }; anchorFilePath field added to Comment, AddCommentInput, Db; listComments extended to accept DiffLineAnchor alongside Anchor
- lib/comments/comment-service.test.ts — 4 new TDD integration tests (add diff line comment, list by diff+file+line anchor, diff isolation, resolve diff line comment)
- app/api/diffs/[id]/comments/route.ts — GET list (optional ?filePath=&lineNumber= filter; 404 if diff missing) + POST create (human session or agent Bearer; validates body/filePath/lineNumber; 201)
- app/diffs/[id]/page.tsx — click any diff line to open comment form; badge indicators on commented lines; click badge to open thread panel with Resolve per comment; Add reply button
- 146/146 tests passing; tsc --noEmit clean

2026-06-19 — Issue 8: Diff Upload and View

Upload and view PR diffs as immutable first-class artifacts.

Deliverables:
- prisma/schema.prisma — Diff model added (id, title, description, branch, diffText, issueId, authorUserId?, authorLabel, createdAt); db push + client regenerated
- lib/diffs/diff-service.ts — Diff interface; uploadDiff, getDiff, listDiffsByIssue; Db interface for testability
- lib/diffs/diff-service.test.ts — 10 TDD integration tests (upload with all fields, agent author, default description, immutability, getDiff full fields, getDiff null on missing, listDiffsByIssue chronological order/empty/isolation/multiple artifacts)
- app/api/diffs/route.ts — POST upload diff (dual auth: human session or agent Bearer; validates title/branch/diffText/issueId; 201)
- app/api/diffs/[id]/route.ts — GET diff detail (human session; 404 on missing)
- app/api/issues/[id]/diffs/route.ts — GET list diffs for issue (human session; 404 if issue missing)
- app/board/[id]/page.tsx — Diffs section with upload form; optimistic append; links to viewer
- app/diffs/[id]/page.tsx — Diff viewer: metadata header, diff body rendered via diff2html (line-by-line, local CSS bundle)
- 142/142 tests passing; tsc --noEmit clean

2026-06-19 — Issue 7: Document Inline Comments

Inline comments anchored to character offset ranges within specific document versions.

Deliverables:
- prisma/schema.prisma — anchorStart Int? and anchorEnd Int? added to Comment model; db push + client regenerated
- lib/comments/comment-service.ts — extended Comment interface with anchorStart/anchorEnd; extended AddCommentInput with optional anchor; extended listComments to accept optional Anchor filter; added resolveComment(db, id); extended Db interface with comment.update and anchor-aware findMany
- lib/comments/comment-service.test.ts — 6 new integration tests (add with anchor, null anchor for issue comments, anchor filter returns matching range, no-anchor returns all, resolve sets status, resolved visible in list); makePgClient extended
- app/api/documents/[id]/comments/route.ts — GET list (with optional ?versionId and anchor params) + POST create inline comment (dual auth; validates versionId, anchorStart, anchorEnd, body)
- app/api/comments/[id]/route.ts — PATCH resolve comment (human session; status must be "resolved")
- app/documents/[id]/page.tsx — text selection captures char offsets; floating comment form on selection; renderAnnotatedContent highlights commented ranges (yellow=open, gray=resolved); click highlight to open thread panel; Resolve button per comment
- 132/132 tests passing; tsc --noEmit clean

2026-06-19 — Issue 6: Document Store Versioning

Append-only version history for documents with diff view.

Deliverables:
- lib/documents/document-service.ts — updateDocument, getDocumentAtVersion, listDocumentVersions, diffDocumentVersions; extended Db interface with document.update, documentVersion.findMany, optional versionNumber in findFirst where; LCS-based computeUnifiedDiff helper
- lib/documents/document-service.test.ts — 17 new TDD integration tests (create v2, v1 immutable, null for missing, successive increments, latest after update, getDocumentAtVersion x4, listDocumentVersions x4, diffDocumentVersions x4)
- app/api/documents/[id]/route.ts — GET ?version=N returns content at that version
- app/api/documents/[id]/versions/route.ts — GET list versions + POST create new version (dual auth)
- app/api/documents/[id]/diff/route.ts — GET ?from=N&to=M returns unified diff
- app/documents/[id]/page.tsx — version history sidebar, View/Compare tabs, diff viewer with syntax highlighting
- 126/126 tests passing; tsc --noEmit clean

2026-06-19 — Issue 5: Document Store (Create and Read)

First-class Document entities with append-only versioning foundation, issue linking, and a read-back UI.

Deliverables:
- prisma/schema.prisma — Document model, DocumentVersion model (unique [documentId, versionNumber]), DocumentIssueLink model (compound PK, cascade)
- lib/documents/document-service.ts — createDocument, getDocument, listDocumentsByIssue, linkDocumentToIssue; Db interface for testability
- lib/documents/document-service.test.ts — 11 TDD integration tests against real PG (create with v1, author, link visible, full fields, null on missing, empty list, cross-issue isolation, multi-issue link, idempotent link)
- app/api/documents/route.ts — POST create (dual auth: human session or agent Bearer; 400/401 validation)
- app/api/documents/[id]/route.ts — GET detail (human session; 404 on missing)
- app/api/issues/[id]/documents/route.ts — GET list docs by issue (human session; 404 if issue missing)
- app/board/[id]/page.tsx — Documents section on issue detail: lists linked docs, inline create form, optimistic append, link to viewer
- app/documents/[id]/page.tsx — Document viewer: title, version badge, timestamp, preformatted content
- 109/109 tests passing; tsc --noEmit clean

2026-06-19 — Issue 4: Issue Comments

Threaded comment system on issues, establishing the shared polymorphic Comment data model for future document and diff comment slices.

Deliverables:
- prisma/schema.prisma — Comment model (targetType, targetId, body, authorUserId, authorLabel, status, createdAt)
- lib/comments/comment-service.ts — addComment, listComments; 7 TDD integration tests (human author, agent author, chronological order, isolation by targetType/targetId, empty results, full field check)
- app/api/issues/[id]/comments/route.ts — GET list (human session) + POST create (dual auth: human session or agent Bearer token); 404/400/401 error handling
- app/board/[id]/page.tsx — comment thread in chronological order with author name and timestamp; inline add-comment textarea form; optimistic append on submit
- 98/98 tests passing; tsc --noEmit clean

2026-06-19 — Issue 3: Issue Tracker Kanban and State Machine

Six-column kanban board with server-enforced state machine, CRUD API, and move UI.

Deliverables:
- prisma/schema.prisma — Issue model (id, title, description, column, timestamps)
- lib/issues/state-machine.ts — canTransition(), transition(); 39 TDD unit tests (all valid/invalid pairs + no-ops)
- lib/issues/issue-service.ts — createIssue, listIssues, getIssue, updateIssue, moveIssue; 12 integration tests against real PG
- app/api/issues/route.ts — GET list (optional ?column filter) + POST create
- app/api/issues/[id]/route.ts — GET detail + PATCH update
- app/api/issues/[id]/move/route.ts — POST move (422 on invalid transition, 404 if not found)
- app/board/page.tsx — six-column kanban board; inline create form; review columns visually distinct
- app/board/[id]/page.tsx — issue detail with column badge and move buttons for valid transitions
- 91/91 tests passing; tsc --noEmit clean

2026-06-19 — Issue 2: Human Auth and API Keys

End-to-end authentication for humans (email/password via NextAuth.js JWT) and agents (opaque API keys as Bearer tokens).

Deliverables:
- lib/auth/bearer.ts — extractBearer() pure function; 7 TDD unit tests
- lib/auth/password.ts — bcrypt hash/verify (already in place, tests passing)
- lib/auth/users.ts — createUser, findUserByEmail, validateUserCredentials; integration tests against real PG
- lib/auth/api-keys.ts — generateApiKey (frg_ prefix), SHA-256 hash, timing-safe verify; unit tests
- lib/auth/api-key-service.ts — createApiKey, listApiKeys, revokeApiKey, findActiveApiKey; integration tests
- lib/auth/nextauth-config.ts — credentials provider + JWT strategy + session.user.id callback
- types/next-auth.d.ts — extends Session and JWT types with id field
- middleware.ts — JWT session guard (Edge-compatible); redirects pages, 401s for API routes
- app/api/auth/[...nextauth]/route.ts — NextAuth handler
- app/api/auth/signup/route.ts — POST signup with validation and duplicate-email 409
- app/api/account/api-keys/route.ts — GET list + POST create (returns raw key once)
- app/api/account/api-keys/[id]/route.ts — DELETE revoke
- app/api/agent/ping/route.ts — Bearer auth test endpoint
- app/login/page.tsx — credentials login form
- app/signup/page.tsx — signup form
- app/account/page.tsx — API key management (generate + revoke; key shown once)
- app/providers.tsx — SessionProvider client wrapper in root layout
- 40/40 tests passing; tsc --noEmit clean

2026-06-18 — Issue 1: Project Scaffold

Initialized Next.js 16 App Router monorepo (TypeScript, Tailwind CSS v3, Prisma 7, PostgreSQL).

Deliverables:
- app/ layout shell with nav (Board, Documents, Diffs, Skills, Context placeholder pages)
- /api/health route returning { status: "ok" }
- prisma/schema.prisma + prisma.config.ts wired to local PostgreSQL
- lib/api/health.ts + lib/api/health.test.ts (TDD, node:test runner)
- 2/2 unit tests passing; tsc --noEmit clean; npm run build succeeds

Environment notes (ARM64 sandbox):
- Uses node:test + --experimental-strip-types instead of Vitest (esbuild SIGILL)
- Uses next build --webpack + WASM SWC instead of Turbopack (native SWC SIGFAULT)
- Uses Tailwind CSS v3 instead of v4 (@tailwindcss/oxide SIGFAULT)
- node_modules/@next/swc-wasm-nodejs installed manually from tarball
