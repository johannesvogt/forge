2026-06-19 — Issue 3: Issue Tracker Kanban and State Machine

Six-column kanban board with server-enforced state machine, CRUD API, and move UI.

Deliverables:
- prisma/schema.prisma — Issue model (id, title, description, column, timestamps)
- lib/issues/state-machine.ts — canTransition(), transition(); 39 TDD unit tests (all valid/invalid pairs + no-ops)
- lib/issues/issue-service.ts — createIssue, listIssues, getIssue, updateIssue, moveIssue; 12 integration tests against real PG
- app/api/issues/route.ts — GET list (optional ?column filter) + POST create
- app/api/issues/[id]/route.ts — GET detail + PATCH update
- app/api/issues/[id]/move/route.ts — POST move (422 on invalid transition, 404 if not found)
- app/board/page.tsx — six-column kanban board; inline create form; review columns visually distinct
- app/board/[id]/page.tsx — issue detail with column badge and move buttons for valid transitions
- 91/91 tests passing; tsc --noEmit clean

2026-06-19 — Issue 2: Human Auth and API Keys

End-to-end authentication for humans (email/password via NextAuth.js JWT) and agents (opaque API keys as Bearer tokens).

Deliverables:
- lib/auth/bearer.ts — extractBearer() pure function; 7 TDD unit tests
- lib/auth/password.ts — bcrypt hash/verify (already in place, tests passing)
- lib/auth/users.ts — createUser, findUserByEmail, validateUserCredentials; integration tests against real PG
- lib/auth/api-keys.ts — generateApiKey (frg_ prefix), SHA-256 hash, timing-safe verify; unit tests
- lib/auth/api-key-service.ts — createApiKey, listApiKeys, revokeApiKey, findActiveApiKey; integration tests
- lib/auth/nextauth-config.ts — credentials provider + JWT strategy + session.user.id callback
- types/next-auth.d.ts — extends Session and JWT types with id field
- middleware.ts — JWT session guard (Edge-compatible); redirects pages, 401s for API routes
- app/api/auth/[...nextauth]/route.ts — NextAuth handler
- app/api/auth/signup/route.ts — POST signup with validation and duplicate-email 409
- app/api/account/api-keys/route.ts — GET list + POST create (returns raw key once)
- app/api/account/api-keys/[id]/route.ts — DELETE revoke
- app/api/agent/ping/route.ts — Bearer auth test endpoint
- app/login/page.tsx — credentials login form
- app/signup/page.tsx — signup form
- app/account/page.tsx — API key management (generate + revoke; key shown once)
- app/providers.tsx — SessionProvider client wrapper in root layout
- 40/40 tests passing; tsc --noEmit clean

2026-06-18 — Issue 1: Project Scaffold

Initialized Next.js 16 App Router monorepo (TypeScript, Tailwind CSS v3, Prisma 7, PostgreSQL).

Deliverables:
- app/ layout shell with nav (Board, Documents, Diffs, Skills, Context placeholder pages)
- /api/health route returning { status: "ok" }
- prisma/schema.prisma + prisma.config.ts wired to local PostgreSQL
- lib/api/health.ts + lib/api/health.test.ts (TDD, node:test runner)
- 2/2 unit tests passing; tsc --noEmit clean; npm run build succeeds

Environment notes (ARM64 sandbox):
- Uses node:test + --experimental-strip-types instead of Vitest (esbuild SIGILL)
- Uses next build --webpack + WASM SWC instead of Turbopack (native SWC SIGFAULT)
- Uses Tailwind CSS v3 instead of v4 (@tailwindcss/oxide SIGFAULT)
- node_modules/@next/swc-wasm-nodejs installed manually from tarball
